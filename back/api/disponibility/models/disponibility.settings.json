{
  "kind": "collectionType",
  "collectionName": "disponibilities",
  "info": {
    "name": "disponibility",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "when": {
      "type": "enumeration",
      "enum": [
        "morning",
        "afternoon",
        "full"
      ],
      "required": false
    },
    "start": {
      "type": "date",
      "required": true
    },
    "end": {
      "type": "date",
      "required": true
    },
    "espace": {
      "via": "disponibilities",
      "model": "espace"
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "punctual",
        "day",
        "period"
      ],
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "available",
        "booked",
        "pending",
        "past"
      ],
      "default": "available",
      "required": true
    }
  }
}
