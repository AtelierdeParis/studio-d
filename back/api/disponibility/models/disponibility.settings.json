{
  "kind": "collectionType",
  "collectionName": "disponibilities",
  "info": {
    "name": "disponibility",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "when": {
      "type": "enumeration",
      "enum": [
        "morning",
        "afternoon",
        "full"
      ],
      "required": false
    },
    "start": {
      "type": "datetime",
      "required": true
    },
    "end": {
      "type": "datetime",
      "required": true
    },
    "espace": {
      "model": "espace",
      "via": "disponibilities"
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "punctual",
        "day",
        "period"
      ],
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "available",
        "booked",
        "pending",
        "past",
        "canceled"
      ],
      "default": "available",
      "required": true
    },
    "booking": {
      "model": "booking",
      "via": "disponibilities"
    }
  }
}
